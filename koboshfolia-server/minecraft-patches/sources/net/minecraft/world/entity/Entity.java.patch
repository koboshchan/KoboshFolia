--- a/net/minecraft/world/entity/Entity.java
+++ b/net/minecraft/world/entity/Entity.java
@@ -283,7 +_,7 @@
     public double yOld;
     public double zOld;
     public boolean noPhysics;
-    public final RandomSource random = SHARED_RANDOM; // Paper - Share random for entities to make them more random
+    public final RandomSource random = com.kobosh.koboshfolia.KoboshConfig.allowClientRngManipulation ? RandomSource.create() : SHARED_RANDOM; // Kobosh - allow-client-rng-manipulation
     public int tickCount;
     private int remainingFireTicks;
     public boolean wasTouchingWater;
@@ -1125,9 +_,15 @@
     private double moveStartX;
     private double moveStartY;
     private double moveStartZ;
+    private boolean boundingBoxChanged = false; // Kobosh - Gale - skip entity move if movement is zero
     // Paper end - detailed watchdog information
 
     public void move(MoverType type, Vec3 movement) {
+        // Kobosh start - Gale - skip entity move if movement is zero
+        if (!this.boundingBoxChanged && movement.equals(Vec3.ZERO)) {
+            return;
+        }
+        // Kobosh end - Gale - skip entity move if movement is zero
         final Vec3 originalMovement = movement; // Paper - Expose pre-collision velocity
         // Paper start - detailed watchdog information
         ca.spottedleaf.moonrise.common.util.TickThread.ensureTickThread("Cannot move an entity off-main");
@@ -5266,6 +_,11 @@
     }
 
     public final void setBoundingBox(AABB bb) {
+        // Kobosh start - Gale - skip entity move if movement is zero
+        if (!this.bb.equals(bb)) {
+            this.boundingBoxChanged = true;
+        }
+        // Kobosh end - Gale - skip entity move if movement is zero
         // CraftBukkit start - block invalid bounding boxes
         double minX = bb.minX,
                 minY = bb.minY,
