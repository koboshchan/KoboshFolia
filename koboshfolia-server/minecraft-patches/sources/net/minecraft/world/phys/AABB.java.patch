--- a/net/minecraft/world/phys/AABB.java
+++ b/net/minecraft/world/phys/AABB.java
@@ -18,6 +_,14 @@
     public final double maxX;
     public final double maxY;
     public final double maxZ;
+    // Kobosh start - Lithium fastutil
+    static {
+        assert Direction.Axis.X.ordinal() == 0;
+        assert Direction.Axis.Y.ordinal() == 1;
+        assert Direction.Axis.Z.ordinal() == 2;
+        assert Direction.Axis.values().length == 3;
+    }
+    // Kobosh end - Lithium fastutil
 
     public AABB(final double minX, final double minY, final double minZ, final double maxX, final double maxY, final double maxZ) {
         this.minX = Math.min(minX, maxX);
@@ -28,8 +_,43 @@
         this.maxZ = Math.max(minZ, maxZ);
     }
 
+    // Kobosh start - Lithium - skip redundant min/max normalisation
+    /**
+     * Creates a box without normalising its bounds.
+     *
+     * <p>The public constructor spends six {@link Math#min}/{@link Math#max} calls ordering its
+     * arguments, which is wasted work for callers that already guarantee {@code min <= max} - most
+     * importantly translation, which cannot change the ordering. Only use this via
+     * {@link #ordered} and only where the ordering is provable; anything that can invert a box
+     * (intersecting, inflating by a negative amount, or the arbitrary-value setters) must keep
+     * going through the normalising constructor.
+     */
+    private AABB(
+        final double minX, final double minY, final double minZ, final double maxX, final double maxY, final double maxZ, final boolean ordered
+    ) {
+        // Enabled with -ea only, so this costs nothing in production. NaN bounds are legal here and
+        // compare false, so they are excluded rather than reported as a violation.
+        assert !(minX > maxX) && !(minY > maxY) && !(minZ > maxZ)
+            : "unordered bounds passed to the unchecked AABB constructor: " + minX + ">" + maxX + " / " + minY + ">" + maxY + " / " + minZ + ">" + maxZ;
+        this.minX = minX;
+        this.minY = minY;
+        this.minZ = minZ;
+        this.maxX = maxX;
+        this.maxY = maxY;
+        this.maxZ = maxZ;
+    }
+
+    private static AABB ordered(
+        final double minX, final double minY, final double minZ, final double maxX, final double maxY, final double maxZ
+    ) {
+        return new AABB(minX, minY, minZ, maxX, maxY, maxZ, true);
+    }
+    // Kobosh end - Lithium - skip redundant min/max normalisation
+
     public AABB(final BlockPos pos) {
-        this(pos.getX(), pos.getY(), pos.getZ(), pos.getX() + 1, pos.getY() + 1, pos.getZ() + 1);
+        // Kobosh start - Lithium - skip redundant min/max normalisation
+        this(pos.getX(), pos.getY(), pos.getZ(), pos.getX() + 1, pos.getY() + 1, pos.getZ() + 1, true);
+        // Kobosh end - Lithium - skip redundant min/max normalisation
     }
 
     public AABB(final Vec3 begin, final Vec3 end) {
@@ -37,15 +_,16 @@
     }
 
     public static AABB of(final BoundingBox box) {
-        return new AABB(box.minX(), box.minY(), box.minZ(), box.maxX() + 1, box.maxY() + 1, box.maxZ() + 1);
+        return ordered(box.minX(), box.minY(), box.minZ(), box.maxX() + 1, box.maxY() + 1, box.maxZ() + 1); // Kobosh - Lithium - skip redundant min/max normalisation - BoundingBox is already ordered
     }
 
     public static AABB unitCubeFromLowerCorner(final Vec3 pos) {
-        return new AABB(pos.x, pos.y, pos.z, pos.x + 1.0, pos.y + 1.0, pos.z + 1.0);
+        return ordered(pos.x, pos.y, pos.z, pos.x + 1.0, pos.y + 1.0, pos.z + 1.0); // Kobosh - Lithium - skip redundant min/max normalisation
     }
 
     public static AABB encapsulatingFullBlocks(final BlockPos pos0, final BlockPos pos1) {
-        return new AABB(
+        // Kobosh start - Lithium - skip redundant min/max normalisation - bounds are ordered here already
+        return ordered(
             Math.min(pos0.getX(), pos1.getX()),
             Math.min(pos0.getY(), pos1.getY()),
             Math.min(pos0.getZ(), pos1.getZ()),
@@ -53,6 +_,7 @@
             Math.max(pos0.getY(), pos1.getY()) + 1,
             Math.max(pos0.getZ(), pos1.getZ()) + 1
         );
+        // Kobosh end - Lithium - skip redundant min/max normalisation - bounds are ordered here already
     }
 
     public AABB setMinX(final double minX) {
@@ -80,11 +_,31 @@
     }
 
     public double min(final Direction.Axis axis) {
-        return axis.choose(this.minX, this.minY, this.minZ);
+        // Kobosh start - Lithium fastutil
+        switch (axis.ordinal()) {
+            case 0:
+                return this.minX;
+            case 1:
+                return this.minY;
+            case 2:
+                return this.minZ;
+        }
+        throw new IllegalArgumentException();
+        // Kobosh end - Lithium fastutil
     }
 
     public double max(final Direction.Axis axis) {
-        return axis.choose(this.maxX, this.maxY, this.maxZ);
+        // Kobosh start - Lithium fastutil
+        switch (axis.ordinal()) {
+            case 0:
+                return this.maxX;
+            case 1:
+                return this.maxY;
+            case 2:
+                return this.maxZ;
+        }
+        throw new IllegalArgumentException();
+        // Kobosh end - Lithium fastutil
     }
 
     @Override
@@ -206,17 +_,19 @@
         double maxX = Math.max(this.maxX, other.maxX);
         double maxY = Math.max(this.maxY, other.maxY);
         double maxZ = Math.max(this.maxZ, other.maxZ);
-        return new AABB(minX, minY, minZ, maxX, maxY, maxZ);
+        return ordered(minX, minY, minZ, maxX, maxY, maxZ); // Kobosh - Lithium - skip redundant min/max normalisation - union of two ordered boxes stays ordered
     }
 
     public AABB move(final double xa, final double ya, final double za) {
-        return new AABB(this.minX + xa, this.minY + ya, this.minZ + za, this.maxX + xa, this.maxY + ya, this.maxZ + za);
+        return ordered(this.minX + xa, this.minY + ya, this.minZ + za, this.maxX + xa, this.maxY + ya, this.maxZ + za); // Kobosh - Lithium - skip redundant min/max normalisation - translation preserves ordering
     }
 
     public AABB move(final BlockPos pos) {
-        return new AABB(
+        // Kobosh start - Lithium - skip redundant min/max normalisation - translation preserves ordering
+        return ordered(
             this.minX + pos.getX(), this.minY + pos.getY(), this.minZ + pos.getZ(), this.maxX + pos.getX(), this.maxY + pos.getY(), this.maxZ + pos.getZ()
         );
+        // Kobosh end - Lithium - skip redundant min/max normalisation - translation preserves ordering
     }
 
     public AABB move(final Vec3 pos) {
