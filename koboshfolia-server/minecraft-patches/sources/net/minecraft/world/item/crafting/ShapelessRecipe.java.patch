--- a/net/minecraft/world/item/crafting/ShapelessRecipe.java
+++ b/net/minecraft/world/item/crafting/ShapelessRecipe.java
@@ -38,11 +_,19 @@
     public static final RecipeSerializer<ShapelessRecipe> SERIALIZER = new RecipeSerializer<>(MAP_CODEC, STREAM_CODEC);
     private final ItemStackTemplate result;
     private final List<Ingredient> ingredients;
+    private final boolean isBukkit; // Kobosh - Leaf - simpler vanilla shapeless comparison
 
     public ShapelessRecipe(
         final Recipe.CommonInfo commonInfo, final CraftingRecipe.CraftingBookInfo bookInfo, final ItemStackTemplate result, final List<Ingredient> ingredients
     ) {
+        this(commonInfo, bookInfo, result, ingredients, false); // Kobosh - Leaf - simpler vanilla shapeless comparison
+    }
+
+    public ShapelessRecipe(
+        final Recipe.CommonInfo commonInfo, final CraftingRecipe.CraftingBookInfo bookInfo, final ItemStackTemplate result, final List<Ingredient> ingredients, boolean isBukkit
+    ) {
         super(commonInfo, bookInfo);
+        this.isBukkit = isBukkit; // Kobosh - Leaf - simpler vanilla shapeless comparison
         this.result = result;
         this.ingredients = ingredients;
     }
@@ -74,6 +_,26 @@
 
     @Override
     public boolean matches(final CraftingInput input, final Level level) {
+        // Kobosh start - Leaf - simpler vanilla shapeless comparison
+        if (!this.isBukkit) {
+            java.util.List<Ingredient> ingredients = com.google.common.collect.Lists.newArrayList(this.ingredients.toArray(new Ingredient[0]));
+
+            inventory:
+            for (int index = 0; index < input.size(); index++) {
+                ItemStack itemStack = input.getItem(index);
+                if (!itemStack.isEmpty()) {
+                    for (int i = 0; i < ingredients.size(); i++) {
+                        if (ingredients.get(i).test(itemStack)) {
+                            ingredients.remove(i);
+                            continue inventory;
+                        }
+                    }
+                    return false;
+                }
+            }
+            return ingredients.isEmpty();
+        }
+        // Kobosh end - Leaf - simpler vanilla shapeless comparison
         // Paper start - Improve exact choice recipe ingredients & unwrap ternary
         if (input.ingredientCount() != this.ingredients.size()) {
             return false;
