--- a/net/minecraft/world/item/BowItem.java
+++ b/net/minecraft/world/item/BowItem.java
@@ -27,7 +_,12 @@
         if (entity instanceof Player player) {
             ItemStack projectile = player.getProjectile(itemStack);
             if (projectile.isEmpty()) {
-                return false;
+                if (com.kobosh.koboshfolia.KoboshConfig.infinityBowWorksWithoutArrows) {
+                    // KoboshFolia - allow shooting without arrows; phantom arrow not consumed from inventory
+                    projectile = new ItemStack(net.minecraft.world.item.Items.ARROW);
+                } else {
+                    return false;
+                }
             }
 
             int timeHeld = this.getUseDuration(itemStack, entity) - remainingTime;
@@ -95,7 +_,7 @@
     public InteractionResult use(final Level level, final Player player, final InteractionHand hand) {
         ItemStack itemStack = player.getItemInHand(hand);
         boolean foundProjectile = !player.getProjectile(itemStack).isEmpty();
-        if (!player.hasInfiniteMaterials() && !foundProjectile) {
+        if (!player.hasInfiniteMaterials() && !foundProjectile && !com.kobosh.koboshfolia.KoboshConfig.infinityBowWorksWithoutArrows) {
             return InteractionResult.FAIL;
         }
 
