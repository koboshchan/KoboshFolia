--- a/net/minecraft/world/item/BowItem.java
+++ b/net/minecraft/world/item/BowItem.java
@@ -29,31 +_,35 @@
         } else {
             ItemStack projectile = player.getProjectile(stack);
             if (projectile.isEmpty()) {
+                if (com.kobosh.koboshfolia.KoboshConfig.infinityBowWorksWithoutArrows) {
+                    // KoboshFolia - allow shooting without arrows; phantom arrow not consumed from inventory
+                    projectile = new ItemStack(net.minecraft.world.item.Items.ARROW);
+                } else {
+                    return false;
+                }
+            }
+            int i = this.getUseDuration(stack, entity) - timeLeft;
+            float powerForTime = getPowerForTime(i);
+            if (powerForTime < 0.1) {
                 return false;
             } else {
-                int i = this.getUseDuration(stack, entity) - timeLeft;
-                float powerForTime = getPowerForTime(i);
-                if (powerForTime < 0.1) {
-                    return false;
-                } else {
-                    List<ItemStack> list = draw(stack, projectile, player);
-                    if (level instanceof ServerLevel serverLevel && !list.isEmpty()) {
-                        this.shoot(serverLevel, player, player.getUsedItemHand(), stack, list, powerForTime * 3.0F, 1.0F, powerForTime == 1.0F, null, powerForTime); // Paper - Pass draw strength
-                    }
-
-                    level.playSound(
-                        null,
-                        player.getX(),
-                        player.getY(),
-                        player.getZ(),
-                        SoundEvents.ARROW_SHOOT,
-                        SoundSource.PLAYERS,
-                        1.0F,
-                        1.0F / (level.getRandom().nextFloat() * 0.4F + 1.2F) + powerForTime * 0.5F
-                    );
-                    player.awardStat(Stats.ITEM_USED.get(this));
-                    return true;
+                List<ItemStack> list = draw(stack, projectile, player);
+                if (level instanceof ServerLevel serverLevel && !list.isEmpty()) {
+                    this.shoot(serverLevel, player, player.getUsedItemHand(), stack, list, powerForTime * 3.0F, 1.0F, powerForTime == 1.0F, null, powerForTime); // Paper - Pass draw strength
                 }
+
+                level.playSound(
+                    null,
+                    player.getX(),
+                    player.getY(),
+                    player.getZ(),
+                    SoundEvents.ARROW_SHOOT,
+                    SoundSource.PLAYERS,
+                    1.0F,
+                    1.0F / (level.getRandom().nextFloat() * 0.4F + 1.2F) + powerForTime * 0.5F
+                );
+                player.awardStat(Stats.ITEM_USED.get(this));
+                return true;
             }
         }
     }
@@ -89,7 +_,7 @@
     public InteractionResult use(Level level, Player player, InteractionHand hand) {
         ItemStack itemInHand = player.getItemInHand(hand);
         boolean flag = !player.getProjectile(itemInHand).isEmpty();
-        if (!player.hasInfiniteMaterials() && !flag) {
+        if (!player.hasInfiniteMaterials() && !flag && !com.kobosh.koboshfolia.KoboshConfig.infinityBowWorksWithoutArrows) {
             return InteractionResult.FAIL;
         } else {
             player.startUsingItem(hand);
