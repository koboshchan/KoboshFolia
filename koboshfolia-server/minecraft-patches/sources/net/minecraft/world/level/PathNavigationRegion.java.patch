--- a/net/minecraft/world/level/PathNavigationRegion.java
+++ b/net/minecraft/world/level/PathNavigationRegion.java
@@ -17,8 +_,8 @@
 import net.minecraft.world.level.chunk.ChunkAccess;
 import net.minecraft.world.level.chunk.ChunkSource;
 import net.minecraft.world.level.chunk.EmptyLevelChunk;
+import net.minecraft.world.level.chunk.LevelChunkSection;
 import net.minecraft.world.level.material.FluidState;
-import net.minecraft.world.level.material.Fluids;
 import net.minecraft.world.phys.AABB;
 import net.minecraft.world.phys.shapes.VoxelShape;
 import org.jspecify.annotations.Nullable;
@@ -30,6 +_,12 @@
     protected boolean allEmpty;
     protected final Level level;
     private final Supplier<Holder<Biome>> plains;
+    // Kobosh - Lithium - flat chunk array for fast O(1) lookups without EmptyLevelChunk allocation
+    private final ChunkAccess[] chunksFlat;
+    private final int xLen;
+    private final int zLen;
+    private final int bottomY;
+    private final int heightInBlocks;
 
     public PathNavigationRegion(Level level, BlockPos centerPos, BlockPos offsetPos) {
         this.level = level;
@@ -41,6 +_,15 @@
         this.chunks = new ChunkAccess[sectionPosX - this.centerX + 1][sectionPosZ - this.centerZ + 1];
         ChunkSource chunkSource = level.getChunkSource();
         this.allEmpty = true;
+        // Kobosh - Lithium - build flat 1D chunk array for fast indexed access
+        this.xLen = this.chunks.length;
+        this.zLen = this.xLen > 0 ? this.chunks[0].length : 0;
+        this.chunksFlat = new ChunkAccess[this.xLen * this.zLen];
+        for (int fx = 0; fx < this.xLen; fx++) {
+            System.arraycopy(this.chunks[fx], 0, this.chunksFlat, fx * this.zLen, this.zLen);
+        }
+        this.bottomY = level.getMinY();
+        this.heightInBlocks = level.getHeight();
 
         for (int i = this.centerX; i <= sectionPosX; i++) {
             for (int i1 = this.centerZ; i1 <= sectionPosZ; i1++) {
@@ -125,22 +_,28 @@
 
     @Override
     public BlockState getBlockState(BlockPos pos) {
-        if (this.isOutsideBuildHeight(pos)) {
+        // Kobosh - Lithium - direct section lookup; avoids EmptyLevelChunk allocation on cache miss
+        int y = pos.getY();
+        if (y < this.bottomY || y >= this.bottomY + this.heightInBlocks) {
             return Blocks.AIR.defaultBlockState();
-        } else {
-            ChunkAccess chunk = this.getChunk(pos);
-            return chunk.getBlockState(pos);
-        }
+        }
+        int chunkX = (pos.getX() >> 4) - this.centerX;
+        int chunkZ = (pos.getZ() >> 4) - this.centerZ;
+        if (chunkX >= 0 && chunkX < this.xLen && chunkZ >= 0 && chunkZ < this.zLen) {
+            ChunkAccess chunk = this.chunksFlat[chunkX * this.zLen + chunkZ];
+            if (chunk != null) {
+                LevelChunkSection section = chunk.getSections()[(y - this.bottomY) >> 4];
+                if (section != null && !section.hasOnlyAir()) {
+                    return section.getBlockState(pos.getX() & 15, y & 15, pos.getZ() & 15);
+                }
+            }
+        }
+        return Blocks.AIR.defaultBlockState();
     }
 
     @Override
     public FluidState getFluidState(BlockPos pos) {
-        if (this.isOutsideBuildHeight(pos)) {
-            return Fluids.EMPTY.defaultFluidState();
-        } else {
-            ChunkAccess chunk = this.getChunk(pos);
-            return chunk.getFluidState(pos);
-        }
+        return this.getBlockState(pos).getFluidState(); // Kobosh - Lithium - derive from optimized getBlockState
     }
 
     @Override
