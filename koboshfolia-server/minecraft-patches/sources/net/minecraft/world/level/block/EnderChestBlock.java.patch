--- a/net/minecraft/world/level/block/EnderChestBlock.java
+++ b/net/minecraft/world/level/block/EnderChestBlock.java
@@ -1,6 +_,7 @@
 package net.minecraft.world.level.block;
 
 import com.mojang.serialization.MapCodec;
+import com.kobosh.koboshfolia.KoboshConfig;
 import net.minecraft.core.BlockPos;
 import net.minecraft.core.Direction;
 import net.minecraft.core.particles.ParticleTypes;
@@ -13,6 +_,7 @@
 import net.minecraft.world.entity.monster.piglin.PiglinAi;
 import net.minecraft.world.entity.player.Player;
 import net.minecraft.world.inventory.ChestMenu;
+import net.minecraft.world.inventory.MenuType;
 import net.minecraft.world.inventory.PlayerEnderChestContainer;
 import net.minecraft.world.item.context.BlockPlaceContext;
 import net.minecraft.world.level.BlockGetter;
@@ -85,7 +_,7 @@
                 enderChestInventory.setActiveChest(enderChestBlockEntity); // Needs to happen before ChestMenu.threeRows as it is required for opening animations
                 if (level instanceof ServerLevel serverLevel && player.openMenu(
                     new SimpleMenuProvider(
-                        (containerId, playerInventory, player1) -> ChestMenu.threeRows(containerId, playerInventory, enderChestInventory), CONTAINER_TITLE
+                        (containerId, playerInventory, player1) -> getEnderChestSixRows(containerId, playerInventory, player, enderChestInventory), CONTAINER_TITLE
                     )
                 ).isPresent()) {
                 // Paper end - Fix InventoryOpenEvent cancellation - moved up;
@@ -99,6 +_,34 @@
             return InteractionResult.SUCCESS;
         }
     }
+
+    // Kobosh start - Barrels and enderchests 6 rows
+    private ChestMenu getEnderChestSixRows(int syncId, net.minecraft.world.entity.player.Inventory inventory, Player player, PlayerEnderChestContainer playerEnderChestContainer) {
+        org.bukkit.craftbukkit.entity.CraftHumanEntity bukkitPlayer = player.getBukkitEntity();
+        if (bukkitPlayer.hasPermission("kobosh.enderchest.rows.six")) {
+            return ChestMenu.sixRows(syncId, inventory, playerEnderChestContainer);
+        } else if (bukkitPlayer.hasPermission("kobosh.enderchest.rows.five")) {
+            return new ChestMenu(MenuType.GENERIC_9x5, syncId, inventory, playerEnderChestContainer, 5);
+        } else if (bukkitPlayer.hasPermission("kobosh.enderchest.rows.four")) {
+            return new ChestMenu(MenuType.GENERIC_9x4, syncId, inventory, playerEnderChestContainer, 4);
+        } else if (bukkitPlayer.hasPermission("kobosh.enderchest.rows.three")) {
+            return ChestMenu.threeRows(syncId, inventory, playerEnderChestContainer);
+        } else if (bukkitPlayer.hasPermission("kobosh.enderchest.rows.two")) {
+            return new ChestMenu(MenuType.GENERIC_9x2, syncId, inventory, playerEnderChestContainer, 2);
+        } else if (bukkitPlayer.hasPermission("kobosh.enderchest.rows.one")) {
+            return new ChestMenu(MenuType.GENERIC_9x1, syncId, inventory, playerEnderChestContainer, 1);
+        }
+
+        return switch (KoboshConfig.defaultEnderChestRows) {
+            case 1 -> new ChestMenu(MenuType.GENERIC_9x1, syncId, inventory, playerEnderChestContainer, 1);
+            case 2 -> new ChestMenu(MenuType.GENERIC_9x2, syncId, inventory, playerEnderChestContainer, 2);
+            case 4 -> new ChestMenu(MenuType.GENERIC_9x4, syncId, inventory, playerEnderChestContainer, 4);
+            case 5 -> new ChestMenu(MenuType.GENERIC_9x5, syncId, inventory, playerEnderChestContainer, 5);
+            case 6 -> ChestMenu.sixRows(syncId, inventory, playerEnderChestContainer);
+            default -> ChestMenu.threeRows(syncId, inventory, playerEnderChestContainer);
+        };
+    }
+    // Kobosh end - Barrels and enderchests 6 rows
 
     @Override
     public BlockEntity newBlockEntity(BlockPos pos, BlockState state) {
