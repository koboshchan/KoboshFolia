--- a/net/minecraft/world/level/block/piston/PistonBaseBlock.java
+++ b/net/minecraft/world/level/block/piston/PistonBaseBlock.java
@@ -54,6 +_,7 @@
     public static final int TRIGGER_DROP = 2;
     public static final int PLATFORM_THICKNESS = 4;
     private static final Map<Direction, VoxelShape> SHAPES = Shapes.rotateAll(Block.boxZ(16.0, 4.0, 16.0));
+    private static final Direction[] ALL_DIRECTIONS = Direction.values(); // Kobosh - Lithium - avoid Direction.values() allocation in hot loops
     private final boolean isSticky;
 
     @Override
@@ -128,8 +_,8 @@
     }
 
     private boolean getNeighborSignal(SignalGetter level, BlockPos pos, Direction direction) {
-        for (Direction direction1 : Direction.values()) {
-            if (direction1 != direction && level.hasSignal(pos.relative(direction1), direction1)) {
+        for (Direction direction1 : ALL_DIRECTIONS) { // Kobosh - Lithium - avoid Direction.values() allocation
+            if (direction1 != Direction.DOWN && level.hasSignal(pos.relative(direction1), direction1)) {
                 return true;
             }
         }
@@ -139,7 +_,7 @@
         } else {
             BlockPos blockPos = pos.above();
 
-            for (Direction direction2 : Direction.values()) {
+            for (Direction direction2 : ALL_DIRECTIONS) { // Kobosh - Lithium - avoid Direction.values() allocation
                 if (direction2 != Direction.DOWN && level.hasSignal(blockPos.relative(direction2), direction2)) {
                     return true;
                 }
